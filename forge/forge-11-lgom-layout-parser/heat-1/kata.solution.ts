// extractRowLabel — 解答

export function extractRowLabel(line: string): string {
  return line
    .replace(/^\d+行目：/, "")
    .replace(/（[^）]*）/g, "")
    .trim();
}

// ── 設計メモ 参考回答（汎用4問 × この kata）──────────────
// A1. 境界: 空文字は "" を返す。行番号のみ（"1行目："）は除去後に "" になる。
//     呼び出し側（parseLGomLayout）が filter(Boolean) で弾くため、
//     この関数では関知しない。
// A2. 保証: 例外を投げない。入力文字列を破壊しない（replace は新文字列を返す）。
// A3. 正規表現2本をメソッドチェーンで繋いだ。split して部分を取り出す方法もあるが、
//     行番号の書式が "/^\d+行目：/" と一様なので正規表現1本で済む。
//     カッコは複数箇所に出るため /g フラグが必要。
// A4. テキストの正規化のみ担う。「このラベルがどのフィルタタグか」の判定は
//     rowLabelToFilterTags が担う。
// ──────────────────────────────────────────────
